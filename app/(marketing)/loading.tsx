import { FullScreenLoading } from "@/components/ui/full-screen-loading"

export default function Loading() {
  return (
    <FullScreenLoading
      description="Preparando contenido de CORTE.Ges."
      label="Cargando pantalla..."
    />
  )
}
