# Private registry (ejemplo)

Este directorio es una base para un registry interno compatible con el modelo de distribución
de componentes/bloques tipo shadcn.

La idea no es publicar todo desde el día 1. La idea es:

1. estabilizar bloques
2. generar metadata
3. exponer el registry cuando sea útil
